/*
 * PDF Helper Stub
 * Fallback implementation (should never be used in practice)
 */

import 'dart:typed_data';

Future<bool> savePdfImpl(Uint8List pdfBytes, String fileName) async {
  throw UnsupportedError('Platform not supported');
}
